title = "Folio"

[extend]
useDefault = true

[allowlist]
description = "Public anon key and non-secret localStorage ids."
paths = [
  '''(^|/)config\.js$''',
  '''(^|/)config\.example\.js$''',
]
regexes = [
  '''folio-month-prefs-v1''',
]
